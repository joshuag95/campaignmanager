import { CampaignDashboard } from "@/components/campaign-dashboard";

// Home route now hosts the initial functional dashboard shell.
export default function Home() {
  return <CampaignDashboard />;
}
